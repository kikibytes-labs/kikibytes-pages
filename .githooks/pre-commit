#!/bin/sh
# Simple pre-commit hook: format and analyze
flutter format .
flutter pub get >/dev/null 2>&1
# run analyze but ignore info-level messages so local style suggestions don't block commits
flutter analyze --no-fatal-infos
if [ $? -ne 0 ]; then
  echo "flutter analyze failed — commit aborted"
  exit 1
fi
exit 0
