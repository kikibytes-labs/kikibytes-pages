{
  "name": "kikibytes-pages",
  "private": true,
  "version": "1.0.0",
  "description": "KikiBytes Labs static website quality checks",
  "scripts": {
    "check": "npm run check:js && npm run check:site",
    "check:js": "node --check site/scripts/site.js",
    "check:site": "node --test tests/site.test.mjs",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
